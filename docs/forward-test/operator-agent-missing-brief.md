# 架空の折り紙fixture 不足資料の例

これは前向き試験専用の架空デモである。材料は厚さ3 mm、印刷面は `PF_PRINT_FRONT` とする。`SIDE` の `FOLD_SIDE_TAB` を折って `BODY` の近くに置く。

この資料には、山折りか谷折りか、完成角、可動側、固定側、完成時の位置と向きが書かれていない。agentは成功扱いにせず、例えば「`SIDE` の `FOLD_SIDE_TAB` は印刷面から見て山折りか谷折りか」「完成内角と可動側はどちらか」「折った `SIDE` を `BODY` のどの辺へどう接するか」を質問する。
